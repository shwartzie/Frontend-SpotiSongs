import React from 'react';
import { NavBar } from './NavBar.tsx';
export const Header = () => {

    return (
        <header className='main-layout-nav-bar'>
            <NavBar />
        </header>
    );
};